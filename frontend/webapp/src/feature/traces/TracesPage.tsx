import { Separator } from '@radix-ui/themes';

// This page does not have project context yet, so keep a fixed version id for now.

const TracesPage = () => {
  return (
    <>
      <div>
        <h1>Version 1</h1>
      </div>
      <Separator size="4" my="4" />
    </>
  );
};

export default TracesPage;
